apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirror-front.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mirror-front.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      appProtocol: http
  selector:
    {{- include "mirror-front.selectorLabels" . | nindent 4 }}
