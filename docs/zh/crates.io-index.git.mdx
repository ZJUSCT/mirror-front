---
mirrorId: crates.io-index.git
isGit: true
---

编辑 `~/.cargo/config` 文件，添加以下内容：

```
[source.crates-io]
replace-with = 'zju'

[source.zju]
registry = "https://mirror.zju.edu.cn/git/crates.io-index.git"
```

该镜像可加快 cargo 读取软件包索引的速度。
