---
mirrorId: pypi
---

> pypi 镜像目前仅为校内反向代理清华 TUNA 镜像站。若您的网络环境处于校外， pip 的 HTTP(S) 请求将会被重定向（ 302 ）至 TUNA 镜像站。

## 临时使用
```bash
pip install -i https://mirror.zju.edu.cn/pypi/web/simple some-package
```
注意，`simple` 不能少，若使用 http 镜像需要加上 `--trusted-host mirror.zju.edu.cn`

## 设为默认
升级 pip 到最新的版本 (>=10.0.0) 后进行配置：

```bash
pip install pip -U
pip config set global.index-url https://mirror.zju.edu.cn/pypi/web/simple
```
如果您到 pip 默认源的网络连接较差，临时使用本镜像站来升级 pip ：
```bash
pip install -i https://mirror.zju.edu.cn/pypi/web/simple pip -U
```
